{
  "name": "dxf-core-build",
  "private": true,
  "version": "0.0.1",
  "description": "three-dxf-viewer를 IIFE 전역 번들(window.DxfCore)로 빌드하는 격리된 서브프로젝트 — public/의 나머지 파일들과 배포 파이프라인에는 영향 없음.",
  "scripts": {
    "postinstall": "patch-package",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "patch-package": "^8.0.0"
  },
  "dependencies": {
    "three": "0.171.0",
    "three-dxf-viewer": "1.0.44"
  }
}
